<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Danh sách sản phẩm</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="p-4">
  <div class="container">
    <h1 class="mb-3"><span>📦</span> Danh sách sản phẩm</h1>
    <a href="/" class="btn btn-link">⬅ Quay lại trang chủ</a>

    <table class="table table-bordered table-hover mt-3">
      <thead class="table-dark">
        <tr>
          <th>ID</th>
          <th>Tên sản phẩm</th>
          <th>Giá</th>
          <th>Hành động</th>
        </tr>
      </thead>
      <tbody>
        <% if (products.length === 0) { %>
          <tr><td colspan="4" class="text-center">Chưa có sản phẩm nào</td></tr>
        <% } else { %>
          <% products.forEach(p => { %>
            <tr>
              <td><%= p.id %></td>
              <td><%= p.name %></td>
              <td><%= Number(p.price).toFixed(3) %>₫</td>
              <td>
                <a href="/edit/<%= p.id %>" class="btn btn-warning btn-sm">✏️ Sửa</a>
                <a href="/delete/<%= p.id %>" class="btn btn-danger btn-sm" onclick="return confirm('Xóa sản phẩm này?')">🗑️ Xóa</a>
              </td>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>
