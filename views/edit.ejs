<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>✏️ Sửa sản phẩm</title>
  <style>
    body { font-family: Arial, sans-serif; text-align: center; background: #f0f7ff; }
    form {
      background: white;
      padding: 30px;
      margin-top: 50px;
      border-radius: 12px;
      display: inline-block;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }
    input, button {
      display: block;
      margin: 10px auto;
      padding: 10px;
      width: 250px;
      border-radius: 5px;
      border: 1px solid #ccc;
    }
    button {
      background: #2196f3;
      color: white;
      cursor: pointer;
    }
    a { text-decoration: none; color: #4caf50; display: block; margin-top: 15px; }
  </style>
</head>
<body>
  <h1>✏️ Sửa sản phẩm</h1>
  <form action="/edit/<%= product.id %>" method="POST">
    <input type="text" name="name" value="<%= product.name %>" required />
    <input type="number" step="0.01" name="price" value="<%= product.price %>" required />
    <button type="submit">Cập nhật</button>
  </form>
  <a href="/products">⬅ Quay lại danh sách</a>
</body>
</html>
